;;; package --- Summary
;;; Commentary:

;; ------ emms 配置 ------

;;; Code:

(require 'emms-setup)

(emms-all)
(emms-standard)
(emms-default-players)

;; (setq emms-player-list '(emms-player-vlc))
;; (emms-default-players)

(provide 'init-emms)

;;; init-emms ends here
